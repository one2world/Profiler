<UserControl x:Class="Unity.MemoryProfiler.UI.Controls.ObjectOrTypeLabel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="24" d:DesignWidth="400">
    <UserControl.Resources>
        <!-- 数据类型指示器样式 -->
        <Style x:Key="DataTypeIndicatorStyle" TargetType="Ellipse">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Height" Value="8"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Height="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/> <!-- 数据类型指示器 -->
            <ColumnDefinition Width="Auto"/> <!-- 类型图标 -->
            <ColumnDefinition Width="*"/>    <!-- 文本标签 -->
        </Grid.ColumnDefinitions>

        <!-- 数据类型指示器（小圆点）-->
        <Ellipse x:Name="DataTypeIndicator" 
                 Grid.Column="0"
                 Style="{StaticResource DataTypeIndicatorStyle}"
                 Fill="#888888"
                 VerticalAlignment="Center"/>

        <!-- 类型图标（预留，当前使用占位符）-->
        <TextBlock x:Name="TypeIconPlaceholder" 
                   Grid.Column="1"
                   Text="📦" 
                   FontSize="14"
                   Margin="0,0,4,0"
                   VerticalAlignment="Center"
                   Visibility="Collapsed"/>

        <!-- 文本标签 -->
        <TextBlock x:Name="LabelText" 
                   Grid.Column="2"
                   Text="No Object Selected"
                   FontSize="12"
                   VerticalAlignment="Center"
                   TextTrimming="CharacterEllipsis"
                   ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
    </Grid>
</UserControl>

